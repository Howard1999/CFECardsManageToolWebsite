extends layout

block content
  - const headerString= title
  - const contentString= signedIn?"歡迎 ! !  "+userName:"歡迎來到牌組管理器"

  h1= headerString
  p= contentString
  -if(!signedIn)
    a(href="/account/google-oauth2/sign-in/") 登入
  -else
    a(href="/account/sign-out/") 登出